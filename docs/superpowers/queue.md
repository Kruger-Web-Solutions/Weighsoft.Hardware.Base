# Initiative Queue

| # | Initiative | Branch | Status | Plan | Notes |
|---|------------|--------|--------|------|-------|
| 1 | All-in-one ESP firmware: lift TFT weight screen + character-LCD onto the trunk as per-board compile-time options | `NewEspAllInOne` | BUILT (4/4 units) — final sweep running | [plan](plans/2026-04-30-multi-mode-serial-phase-1.md), [display-integration](plans/2026-06-14-newespallinone-display-integration.md), [handoff](specs/2026-06-14-newespallinone-handoff-spec.md) | Off `SerialReaderWriter` trunk. ✅ U1 scaffold (a68e3b0). ✅ S3 USB-CDC writer fix (87c2605). ✅ U2 remote-weight receiver, FT_REMOTE_WEIGHT (7c16c0b). ✅ U3 TFT screen `tftdisplay`, FT_TFT_WEIGHT_SCREEN, +esp32dev_tft/cyd_tft envs (f2ed4eb). ✅ U4 character-LCD `lcddisplay`, FT_DISPLAY_LCD, vendored LiquidCrystal_I2C, UI tabs, +esp32dev_lcd env (fe3c69c) — esp32dev_lcd green, tsc clean. **DONE & VERIFIED:** target boards all green (esp32s3, esp32dev_tft, cyd_tft, esp32dev_lcd) + defaults (nodemcu, firebeetle); UI tsc clean. **Stability audit done** (read-only multi-agent): BLE was only commented off on the display envs, never set — fixed with `-D FT_BLE=0` on all three (2e0458f, removes an LCD BLE-bridge freeze + WiFi radio contention, −800KB); weight-forwarder given 2s HTTP timeouts so a stalled POST can't trip the watchdog (2163d56); LCD serial-bridge now coalesces to the latest line + skips per-line reconnect (2651662). **esp32dev partition fixed** (partitions_custom→partitions_ble). **PR → `SerialReaderWriter`** (clean ~10-commit review; SerialReaderWriter→master is a separate, larger merge). Remaining pre-existing: `esp12e` (ESP8266 — trunk is ESP32-only; PO decision: full port or drop from CI). Serial execution, no worktrees. Sender multi-URL rewrite OUT of scope. |
