.pio
.clang_complete
.gcc-flags.json
*Thumbs.db
/data/www
/lib/framework/WWWData.h

# Device runtime config - NEVER commit. These are written by the board and hold
# live credentials: wifiSettings.json (WiFi password), securitySettings.json
# (JWT secret + user passwords), apSettings/otaSettings/mqttSettings (passwords).
# A WiFi password reached the published history this way once already (2026-04-23).
/data/config/
/interface/build
/interface/node_modules
/interface/.eslintcache
.vscode
# Claude Code local noise — .claude/skills/ IS committed (remote agents load skills from the clone)
.claude/settings.local.json
.claude/launch.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.superpowers/

.playwright-cli/
