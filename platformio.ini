[platformio]
extra_configs = 
  factory_settings.ini
  features.ini
;default_envs = esp12e
default_envs = esp32s3

[env]
build_flags=
  ${factory_settings.build_flags}
  ${features.build_flags}
  -D NO_GLOBAL_ARDUINOOTA
  ; Uncomment PROGMEM_WWW to enable the storage of the WWW data in PROGMEM
  -D PROGMEM_WWW
  ; Uncomment to configure Cross-Origin Resource Sharing
  ;-D ENABLE_CORS
  ;-D CORS_ORIGIN=\"*\"

; ensure transitive dependencies are included for correct platforms only
lib_compat_mode = strict

; OTA upload configuration for ESP32-S3 (uncomment when using wireless OTA)
; Set upload_port to your device IP (check web interface if changed)
;upload_flags =
; --port=8266
; --auth=esp-react
upload_port = 192.168.3.55
upload_protocol = espota

framework = arduino
monitor_speed = 115200

extra_scripts = 
 pre:scripts/build_interface.py

lib_deps =
  ArduinoJson@>=6.0.0,<7.0.0
  ; The following allows the use of the latest code for ESPAsyncWebServer - there hasn't been a release in a while
  ; Work around for https://github.com/me-no-dev/ESPAsyncWebServer/issues/1151
  https://github.com/me-no-dev/ESPAsyncWebServer
  ;ESP Async WebServer@>=1.2.0,<2.0.0
  AsyncMqttClient@>=0.9.0,<1.0.0
  links2004/WebSockets@^2.5.4
  
[env:esp12e]
platform = espressif8266
board = esp12e
board_build.f_cpu = 160000000L
board_build.filesystem = littlefs

[env:node32s]
; Partition scheme with BLE support (NO OTA)
; Single factory partition: 3.3MB app, 640KB filesystem
; See docs/CONFIGURATION.md "OTA Limitations" for why OTA is not possible with BLE
board_build.partitions = partitions_ble.csv
platform = espressif32
board = node32s
board_build.filesystem = littlefs
upload_protocol = esptool
upload_port = COM11
; Increase watchdog timeout for large PROGMEM web interface
build_flags =
  ${env.build_flags}
  -DCONFIG_ESP_TASK_WDT_TIMEOUT_S=10

[env:esp32dev]
; Generic ESP32 development board for troubleshooting
board_build.partitions = partitions_custom.csv
platform = espressif32
board = esp32dev
board_build.filesystem = littlefs
; Increase watchdog timeout and enable verbose output
build_flags =
  ${env.build_flags}
  -DCONFIG_ESP_TASK_WDT_TIMEOUT_S=10
  -DCORE_DEBUG_LEVEL=5

[env:esp32s3]
; ESP32-S3 board configuration (8MB flash)
; Supports native USB, more RAM (512KB), dual-core
; BLE + OTA supported — 8MB flash fits dual 2.5MB app slots
platform = espressif32
board = esp32-s3-devkitc-1
board_build.filesystem = littlefs
board_build.partitions = partitions_ble_ota.csv
; ESP32-S3 specific settings
build_flags =
  ${env.build_flags}
  -DCONFIG_ESP_TASK_WDT_TIMEOUT_S=10
  ; Keep CDC_ON_BOOT=0 so `Serial` stays on UART0 (logs unchanged on the on-board
  ; UART USB-C port). Native USB CDC is brought up manually in `src/main.cpp` as a
  ; dedicated forwarder data sink (`USBCDC dataUsbCdc`) on the ESP32-S3 USB-OTG
  ; port - this gives PC-side scale tools a clean weight-only stream without any
  ; log refactor. See docs/SERIAL-WRITER-EXAMPLE.md for the dual-port architecture.
  -DARDUINO_USB_CDC_ON_BOOT=0
  -DARDUINO_USB_MODE=0

; Uncomment for USB upload (comment for OTA)
upload_protocol = esptool
upload_port = COM11


